1.Create an array to store all HTML elements that user can select from drop down
2.Selecting elements from HTML
    a.Container for class = “options”
    b.Container for class = “output”
    c.Container that holds tag(drop down options)
    d.Container to store the input from paragraphs slider
    e.Container to store the input from the words slider
    f.Container that displays the current paragraph count
    g.Container that displays the current words count
3.Create a function to initialise dropdown add event listeners and prepare the website for user interaction
    a.Loop through each tag in tagOptions(.1) and create <options> element dynamically
    Store the tag name in value(<option value = “<p>”</option> if p is selected)
    b.The browser automatically makes the selected option visible to user
    c.Add the tags to ((3)(a))
    d.Add event listener to ((2)(d)) which will update the displayed paragraph count when the user moves the slider
    e.Add event listener to ((2)(e)) which will update the displayed words count as the user moves the slider
    f.Select the generate button and attach a click event listener
        i.Add event listener to it which will call the(6) that will start the process
4.Create a function that will update the number shown on the screen every time the paragraph slider is moved
5.Create a function that will update the number shown on the screen every time the words slider is moved
6.Create a function that collects all user inputs and sends it to (7) and displays the output
    a.Convert the paragraph slider value(which is in string by default) to number
    b.Get the selected HTML tag from the drop down and store it in a container
    c.Create a container that will store the value from include HTML drop down
        d.Convert the word slider value(which is in string by default) to number
    e.Store the text generated by (7)
    f.Display the text
7.Create a function that builds paragraphs based on user input
    a.Create a container placeholder that has dummy text
    b.Create an array with empty strings, one slot for one paragraph and so on..
    c.Loop once for each paragraph
        i.Generate words for current paragraph
        ii.If HTML is included wrap words in a tag else plain text
        iii.Combine all paragraph in one string and separate by new line
8.Create a function that returns only the number of words requested by user
    a.Create a container that store large lorem text
    b.Split the text into array of words
    c.If requested words are within available range, return only required words else return entire text
9.Create a function that inserts the generated text into output container
10.Call (3)
